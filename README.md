# odin-recipes

<!-- Following The Odin Project curriculum, this project is an assignment to create a basic HTML website with links to a few recipes.

-Created an index.html with links to three recipes
-Created three individual recipe pages with images of the finished products, a brief description of the item being prepared, an unordered list containing the recipe ingredients, and an ordered list containing the steps to prepare the recipe.


Issues to resolve:

1. VS Code doesn't seem to be functioning the same in my Linux boot as it does in my Windows environment. Added the Live Server and Prettier extensions, as I have in the Windows install, but the IDE won't format the code on save (despite adjusting the setting).

2. VS Code seems to recognize the root that I'm referencing all links from as the parent directory of where the project files are actually stored, ie. expected to link to /recipes/filename.html but had to link to /odin-recipes/recipes/filename.html instead. The index.html file is in the odin-recipes folder and the linked files are all within the recipes folder. -->
